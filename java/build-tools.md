# Java : Build Tools

## Maven

```bash
mvn --version
mvn clean verify
mvn dependency:tree
```

## Gradle Wrapper

```bash
./gradlew --version
./gradlew clean build
./gradlew dependencies
```

Commit the selected build wrapper, pin plugins, keep reproducible configuration, separate build and runtime dependencies, and run the same verification lifecycle locally and in CI.
